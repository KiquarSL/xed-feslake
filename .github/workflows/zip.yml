name: Zip theme

on:
    push:
        branches: ["main"]
    pull_request:
        branches: ["main"]

jobs:
    build:
        run-on: ununtu-latest
        steps:
            - uses: "actions/upload-artifact@v4"
              name: "feslake-theme"
              path: |
                  README.md
                  theme.json
